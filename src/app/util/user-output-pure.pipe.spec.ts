import { UserOutputPurePipe } from './user-output-pure.pipe';

describe('UserOutputPurePipe', () => {
  it('create an instance', () => {
    const pipe = new UserOutputPurePipe();
    expect(pipe).toBeTruthy();
  });
});
