import { UserOutputPipe } from './user-output.pipe';

describe('UserOutputPipe', () => {
  it('create an instance', () => {
    const pipe = new UserOutputPipe();
    expect(pipe).toBeTruthy();
  });
});
